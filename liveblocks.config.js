// Define Liveblocks configuration for your application

const config = {
  Presence: {
    cursor: { x: null, y: null },
  },
};

// No need for `declare global` or `export` in JavaScript.
module.exports = config;
